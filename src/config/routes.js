import HomePage from '@/components/pages/HomePage'

export const routes = [
  {
    id: 'tasks',
    label: 'Tasks',
    path: '/tasks',
    icon: 'CheckSquare',
    component: HomePage
  }
]

export default routes